<div class="mt-2 flex flex-col gap-2 items-center justify-center w-full">
  <%= render "paginator" %>
  <% @reviews.each do |review| %>
    <%= render "review", review: review, review_average: @score_averages[review.id] %>
  <% end %>
</div>